<div class="color-white">

  <%= simple_form_for list do |f| %>
      <%= f.input :name, class: 'color-white' %>
      <%= f.submit "Create list", class: 'btn btn-primary border' %>
  <% end %>

</div>
